// Shared flight contract for server damage and client fire-stream visuals.
export const FLAME_RULES = Object.freeze({
  speed: 30, range: 18, cadence: 0.05, coneDeg: 12,
  radius: 0.12, radiusGrowth: 0.035, maxProjectiles: 512,
});
